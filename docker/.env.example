# SonarQube configuration for docker compose
# Copy to .env and customize before running docker compose up -d

# Port to expose SonarQube UI on the host (default: 9000)
SONARQUBE_PORT=9000

# PostgreSQL password (change before production use)
POSTGRES_PASSWORD=sonar
